{
  "name": "etro-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "concurrently --names v1,v2 -c \"bgBlue.bold,bgMagenta.bold\" --kill-others-on-fail \"cd v1 && npm run build\" \"cd v2 && npm run build\" && rm -r build && mv v1/public build && mv v2/build build/v2",
    "prepublish": "npm run build",
    "publish": "gh-pages -d public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/etro-js/etro-js.github.io.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/etro-js/etro-js.github.io/issues"
  },
  "homepage": "https://github.com/etro-js/etro-js.github.io#readme",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
